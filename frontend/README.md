# Task Manager — Frontend

Vue 3 SPA for the Task Manager app. See the [project README](../README.md) for the full documentation (setup, API reference, architecture).

Quick start:

```bash
npm install
npm run dev
```

Runs at `http://localhost:5173`, expects the backend API at `http://localhost:8000`.
