# Task Manager — Backend

Laravel 12 API for the Task Manager app. See the [project README](../README.md) for the full documentation (setup, API reference, architecture).

Quick start:

```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate --seed
php artisan serve
```

Seeded admin login: `admin@example.com` / `password`.
